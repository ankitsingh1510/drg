export type userParams = {
  userMasterId: number;
};

export interface UserDetails {
  userName: string;
  userId: string;
  userEmail: string;
  fName: string;
  lName: string;
  userRole: string;
}
